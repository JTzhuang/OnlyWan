---
phase: 19-i-o-npy-pt-goal-npy-pt-python
task: inference_tests
total_tasks: 5
status: in_progress
last_updated: 2026-03-27T14:00:00Z
---

<current_state>
Phase 19 Plan 01 execution complete. Added .npy I/O utilities to all 4 model test files (CLIP, T5, VAE, Transformer) with inference tests using random data. All tests compile successfully. Currently fixing memory issues in test_clip.cpp for inference execution.
</current_state>

<completed_work>

- ✅ Phase 19 Plan 01 executed (ce146ca)
- ✅ libnpy integrated into thirdparty (90b2dc2)
- ✅ test_io_utils.hpp created with load_npy/save_npy (224e95c)
- ✅ test_io_npy.cpp with 10/10 tests passing (4995f04)
- ✅ test_data directory with generate_test_data.py (d4c5a92)
- ✅ Added npy_io tests to all 4 model files (b26165b)
- ✅ Added inference tests with random data (d68017d)
- ✅ Fixed GGMLRunnerContext usage (bb164c4)
- ⏳ Memory fix for CLIP inference (test_clip.cpp line 131: increased from 10MB to 100MB)

</completed_work>

<remaining_work>

- Compile test_clip with increased memory (100MB)
- Run all 4 model tests to verify inference works
- Commit memory fix
- Verify .npy output files are created correctly

</remaining_work>

<decisions_made>

- Used GGMLRunnerContext for all model forward/compute calls (required by API)
- Increased CLIP context memory to 100MB to handle full inference (was 10MB, insufficient)
- Kept VAE, T5, Transformer at their current memory sizes (they work)
- All outputs saved as .npy files for Python comparison

</decisions_made>

<blockers>

- CLIP inference requires more memory than initial 10MB allocation
- Workaround: Increased to 100MB in test_clip.cpp (line 131)

</blockers>

<context>

The goal is to enable numerical consistency comparison between C++ model outputs and Python reference implementations. Each model test now:
1. Generates random input data
2. Runs inference through the model
3. Saves output to .npy file
4. Can be compared with Python-generated reference .npy files

All 4 models (CLIP, T5, VAE, Transformer) have complete test frameworks with:
- Model initialization tests
- Registry tests
- .npy I/O roundtrip tests
- Inference tests with random data

The inference tests were failing due to incorrect GGMLRunnerContext usage (passing ggml_context* instead of GGMLRunnerContext*). This was fixed in commit bb164c4.

CLIP inference was failing due to insufficient memory. Fixed by increasing context from 10MB to 100MB.

</context>

<next_action>

1. Compile test_clip with memory fix: `cmake --build build --target test_clip`
2. Run all 4 tests: `./build/bin/test_clip && ./build/bin/test_t5 && ./build/bin/test_vae && ./build/bin/test_transformer`
3. Verify .npy files are created in /tmp/
4. Commit memory fix: `git add tests/cpp/test_clip.cpp && git commit -m "fix(tests): increase CLIP context memory to 100MB for inference"`

</next_action>
