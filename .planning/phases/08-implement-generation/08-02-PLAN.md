---
phase: 08-implement-generation
plan: 02
type: execute
wave: 2
depends_on: ["08-01"]
files_modified:
  - src/api/wan-api.cpp
autonomous: true
requirements:
  - API-03
  - API-04

must_haves:
  truths:
    - "wan_generate_video_t2v_ex returns WAN_SUCCESS (not WAN_ERROR_UNSUPPORTED_OPERATION)"
    - "wan_generate_video_i2v_ex returns WAN_SUCCESS (not WAN_ERROR_UNSUPPORTED_OPERATION)"
    - "Output AVI file is non-empty and starts with RIFF....AVI signature"
    - "Output frame count equals ((num_frames-1)/4*4)+1 temporal expansion"
  artifacts:
    - path: "src/api/wan-api.cpp"
      provides: "Full Euler denoising loop, CFG, latent normalization, VAE decode, AVI write for T2V and I2V"
      contains: "wan_latents_mean"
    - path: "src/api/wan-api.cpp"
      provides: "I2V VAE-encode of input image to c_concat latent"
      contains: "vae_runner->compute"
  key_links:
    - from: "wan_generate_video_t2v_ex"
      to: "WanRunner::compute"
      via: "Euler loop calling compute per step with sigma timestep tensor"
      pattern: "wan_runner->compute"
    - from: "wan_generate_video_t2v_ex"
      to: "WanVAERunner::compute"
      via: "decode=true after process_latent_out"
      pattern: "vae_runner->compute"
    - from: "wan_generate_video_t2v_ex"
      to: "create_mjpg_avi_from_rgb_frames"
      via: "float->uint8 conversion then AVI write"
      pattern: "create_mjpg_avi_from_rgb_frames"
---

<objective>
Implement the full denoising pipeline in wan_generate_video_t2v_ex and wan_generate_video_i2v_ex:
Euler flow-matching sampler loop, CFG unconditional pass, latent normalization (process_latent_in/out),
VAE decode, float-to-uint8 frame conversion, and AVI write.

Purpose: These are the two remaining WAN_ERROR_UNSUPPORTED_OPERATION stubs. After this plan both
T2V and I2V generation return WAN_SUCCESS and produce a valid AVI file.
Output: src/api/wan-api.cpp with fully implemented generation functions.
</objective>

<execution_context>
@/home/jtzhuang/.claude/get-shit-done/workflows/execute-plan.md
@/home/jtzhuang/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-implement-generation/08-CONTEXT.md
@.planning/phases/08-implement-generation/08-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From include/wan-cpp/wan-internal.hpp (wan_context struct):
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

From src/wan.hpp (WanRunner::compute signature):
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

From src/wan.hpp (WanVAERunner::compute signature):
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

From include/wan-cpp/wan.h (wan_params_t fields used):


From examples/cli/avi_writer.h (implemented in Plan 01):


Existing T5 encode pattern (already in wan_generate_video_t2v_ex, lines 350-388 of wan-api.cpp):
- work_ctx (64MB) for tokenization scratch
- output_ctx (256MB) for T5 output tensor
- ctx->t5_embedder->tokenize(prompt, 512, true) -> tokens, weights, attention_mask
- ctx->t5_embedder->model.compute(n_threads, input_ids, attn_mask, &context, output_ctx)

Existing CLIP encode pattern (already in wan_generate_video_i2v_ex, lines 440-473 of wan-api.cpp):
- ctx->clip_runner->compute(n_threads, pixel_values, false, -1, &clip_fea, output_ctx)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement T2V denoising loop in wan_generate_video_t2v_ex</name>
  <files>src/api/wan-api.cpp</files>
  <read_first>
    - src/api/wan-api.cpp (full function wan_generate_video_t2v_ex, lines ~334-404)
    - include/wan-cpp/wan.h (exact field names on wan_params_t: steps, cfg_scale or cfg, seed, width, height, num_frames, fps)
    - examples/cli/avi_writer.h (create_mjpg_avi_from_rgb_frames signature)
    - ../src/stable-diffusion.cpp lines 2424-2427 (WAN 16-channel mean/std arrays) and lines 2340-2500 (sample_euler / process_latent_in/out / decode_first_stage pattern)
  </read_first>
  <action>
Replace the body of wan_generate_video_t2v_ex in src/api/wan-api.cpp after the existing T5 encode
block (which produces  in ). Keep the T5 encode block unchanged. Replace
everything from the "WanRunner::compute stub" comment to the final return statement.

The replacement implements:

1. LATENT SHAPE
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

2. SIGMA SCHEDULE (linear flow matching, 1.0 -> 0.0)
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

3. WAN 16-CHANNEL MEAN/STD ARRAYS (from stable-diffusion.cpp:2424-2427)
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"
Place these as static const arrays at file scope (before the function, or at top of file scope
section) so they are shared by T2V and I2V. Do NOT redefine them twice.

4. ALLOCATE DENOISING WORK CONTEXT
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

5. INITIALIZE NOISE LATENT with randn * sigmas[0] (=1.0)
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"
Add  near the top of wan-api.cpp if not already present.

6. PROCESS_LATENT_IN: normalize x channel-wise
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

7. UNCONDITIONAL T5 CONTEXT for CFG: encode empty string
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

8. EULER LOOP
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"
Note: the in-place Euler update on x->data is safe because x lives in denoise_ctx (not step_ctx).
The flat loop over ggml_nelements(x) is valid only when x is contiguous (freshly allocated tensor
with default strides) — which it is here.

9. PROCESS_LATENT_OUT: inverse normalize
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

10. VAE DECODE
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

11. FLOAT -> UINT8 FRAME CONVERSION
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

12. AVI WRITE
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

Also add at the top of wan-api.cpp (after existing includes):
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"
(Only add includes not already present.)

IMPORTANT: Check the actual field name for CFG scale in wan_params_t (wan.h) — it may be
, , or . Use whatever name is declared there.
  </action>
  <verify>
    <automated>grep -c "wan_latents_mean" /home/jtzhuang/projects/stable-diffusion.cpp/wan/src/api/wan-api.cpp</automated>
    <automated>grep -c "vae_runner->compute" /home/jtzhuang/projects/stable-diffusion.cpp/wan/src/api/wan-api.cpp</automated>
    <automated>grep -c "create_mjpg_avi_from_rgb_frames" /home/jtzhuang/projects/stable-diffusion.cpp/wan/src/api/wan-api.cpp</automated>
    <automated>grep -c "WAN_ERROR_UNSUPPORTED_OPERATION" /home/jtzhuang/projects/stable-diffusion.cpp/wan/src/api/wan-api.cpp</automated>
  </verify>
  <done>
    - wan_latents_mean/std arrays present in wan-api.cpp
    - vae_runner->compute called with decode=true
    - create_mjpg_avi_from_rgb_frames called in T2V path
    - grep count of WAN_ERROR_UNSUPPORTED_OPERATION in wan-api.cpp is 1 (only I2V stub remains before Task 2)
    - wan_generate_video_t2v_ex returns WAN_SUCCESS on the happy path
  </done>
</task>

<task type="auto">
  <name>Task 2: Implement I2V denoising loop in wan_generate_video_i2v_ex</name>
  <files>src/api/wan-api.cpp</files>
  <read_first>
    - src/api/wan-api.cpp (full function wan_generate_video_i2v_ex, lines ~413-489)
    - include/wan-cpp/wan.h (wan_params_t field names, wan_image_t struct)
    - ../src/stable-diffusion.cpp (encode_first_stage / generate_init_latent / I2V c_concat pattern)
  </read_first>
  <action>
Replace the body of wan_generate_video_i2v_ex after the existing CLIP encode block (which produces
 in ). Keep the CLIP encode block unchanged. Replace everything from the
"WanRunner::compute with real clip_fea" comment to the final return statement.

The I2V pipeline differs from T2V in two ways:
  a) VAE-encode the input image to get  (image latent for conditioning)
  b) Pass  and  to WanRunner::compute in the Euler loop

Step-by-step:

1. VAE-ENCODE INPUT IMAGE to get image latent (c_concat)
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"
Note: VAE encode (decode=false) produces latent shape [iW/8, iH/8, 1, 16].
The c_concat tensor lives in img_enc_ctx — keep img_enc_ctx alive through the Euler loop.

2. LATENT SHAPE, SIGMA SCHEDULE, MEAN/STD ARRAYS
Same as T2V (wan_latents_mean/std are file-scope statics defined in Task 1 — do NOT redefine).
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

3. ALLOCATE DENOISE CONTEXT, INITIALIZE NOISE LATENT, PROCESS_LATENT_IN
Identical to T2V Task 1 steps 4-6.

4. UNCONDITIONAL T5 CONTEXT for CFG
I2V also uses T5 for text conditioning. If ctx->t5_embedder is non-null, encode empty string
same as T2V. If null (pure I2V model with no text), set uncond_context = nullptr and skip CFG
(cfg_scale effectively = 1.0).

5. EULER LOOP — same structure as T2V but pass clip_fea and c_concat:
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

6. PROCESS_LATENT_OUT, VAE DECODE, CLAMP, FLOAT->UINT8, AVI WRITE
Identical to T2V Task 1 steps 9-12.

7. CLEANUP
# 0 "<stdin>"
# 0 "<built-in>"
# 0 "<command-line>"
# 1 "/usr/include/stdc-predef.h" 1 3 4
# 0 "<command-line>" 2
# 1 "<stdin>"

After this task, grep count of WAN_ERROR_UNSUPPORTED_OPERATION in wan-api.cpp must be 0.
  </action>
  <verify>
    <automated>grep -c "WAN_ERROR_UNSUPPORTED_OPERATION" /home/jtzhuang/projects/stable-diffusion.cpp/wan/src/api/wan-api.cpp</automated>
    <automated>grep -c "c_concat" /home/jtzhuang/projects/stable-diffusion.cpp/wan/src/api/wan-api.cpp</automated>
    <automated>cmake --build /home/jtzhuang/projects/stable-diffusion.cpp/wan/build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - WAN_ERROR_UNSUPPORTED_OPERATION no longer appears in wan-api.cpp
    - c_concat VAE-encode path present in wan_generate_video_i2v_ex
    - cmake --build succeeds (0 errors)
    - wan_generate_video_i2v_ex returns WAN_SUCCESS on the happy path
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. grep -c "WAN_ERROR_UNSUPPORTED_OPERATION" src/api/wan-api.cpp returns 0
2. grep -c "wan_latents_mean" src/api/wan-api.cpp returns 1 (defined once, used in both functions)
3. grep -c "vae_runner->compute" src/api/wan-api.cpp returns >= 3 (T2V decode, I2V encode, I2V decode)
4. cmake --build /home/jtzhuang/projects/stable-diffusion.cpp/wan/build 2>&1 | grep -c " error:" returns 0
5. Output AVI file (if model available): file out.avi shows "RIFF" and size > 0
</verification>

<success_criteria>
- Both wan_generate_video_t2v_ex and wan_generate_video_i2v_ex return WAN_SUCCESS
- Euler loop calls WanRunner::compute once per step (conditional) plus once for CFG unconditional
- Latent normalization uses the exact 16-element mean/std arrays from stable-diffusion.cpp:2424-2427
- VAE decode called with decode=true, output clamped to [0,1] via ggml_ext_tensor_clamp_inplace
- AVI written via create_mjpg_avi_from_rgb_frames (implemented in Plan 01)
- Library compiles without errors
</success_criteria>

<output>
After completion, create 
</output>
