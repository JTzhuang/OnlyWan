---
phase: quick
plan: 260328-m62
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/API_CALLS.md
  - docs/examples/model_registry_usage.cpp
  - docs/examples/io_utils_usage.cpp
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Developers can see complete API call examples for model registry"
    - "Developers can see complete API call examples for I/O utilities"
    - "All 9 registered models have usage examples"
    - "Common patterns (BackendRAII, tensor creation, I/O) are documented"
  artifacts:
    - path: "docs/API_CALLS.md"
      provides: "Comprehensive API documentation with call signatures and examples"
    - path: "docs/examples/model_registry_usage.cpp"
      provides: "Runnable examples for all model types (CLIP, T5, WAN VAE, WAN Transformer)"
    - path: "docs/examples/io_utils_usage.cpp"
      provides: "Runnable examples for .npy I/O operations"
  key_links:
    - from: "docs/API_CALLS.md"
      to: "tests/cpp/test_factory.cpp"
      via: "reference patterns"
      pattern: "ModelRegistry::instance()->create"
    - from: "docs/examples/model_registry_usage.cpp"
      to: "src/model_registry.hpp"
      via: "API signatures"
      pattern: "has_version|create"
    - from: "docs/examples/io_utils_usage.cpp"
      to: "tests/cpp/test_io_npy.cpp"
      via: "reference patterns"
      pattern: "load_npy|save_npy"
---

<objective>
Generate comprehensive API call documentation combining model registry and I/O utilities, with runnable examples extracted from test patterns.

Purpose: Developers need clear, copy-paste-ready examples for the two main APIs (model creation and tensor I/O).
Output: API_CALLS.md + two example .cpp files showing all common patterns.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@src/model_registry.hpp
@src/model_factory.hpp
@tests/cpp/test_factory.cpp
@tests/cpp/test_io_npy.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract and document model registry API patterns</name>
  <files>docs/API_CALLS.md</files>
  <action>
Create docs/API_CALLS.md with two main sections:

**Section 1: Model Registry API**
- Document ModelRegistry::instance() singleton access
- Document has_version<T>(string) — check if version exists
- Document create<T>(version, backend, use_mmap, tensor_map, prefix) — create runner
- List all 9 registered models with their version strings:
  * CLIP: "clip-vit-l-14", "clip-vit-h-14", "clip-vit-bigg-14"
  * T5: "t5-standard", "t5-umt5"
  * WAN VAE: "wan-vae-t2v-encode", "wan-vae-t2v-decode"
  * WAN Transformer: "wan-transformer-t2v"
- Show BackendRAII pattern for lifecycle management
- Show String2TensorStorage empty_map pattern for tests

**Section 2: I/O Utilities API**
- Document load_npy(path, ctx) — load .npy file to ggml tensor
- Document save_npy(path, tensor) — save ggml tensor to .npy file
- Document supported dtypes: F32, F16, I32, I64
- Document dimension mapping: ggml ne[] reversed from NumPy shape
- Show GGMLCtxRAII pattern for context lifecycle
- Show TempFile pattern for test cleanup

Include function signatures, parameter descriptions, return types, and error handling notes.
  </action>
  <verify>
File exists with both sections, all 9 models listed, all API functions documented with signatures.
  </verify>
  <done>docs/API_CALLS.md created with complete API reference for both model registry and I/O utilities</done>
</task>

<task type="auto">
  <name>Task 2: Create model registry usage examples</name>
  <files>docs/examples/model_registry_usage.cpp</files>
  <action>
Create docs/examples/model_registry_usage.cpp with runnable examples extracted from test_factory.cpp patterns:

**Example 1: CLIP Text Model**
- Initialize BackendRAII guard
- Call ModelRegistry::instance()->has_version<CLIPTextModelRunner>("clip-vit-l-14")
- Call create<CLIPTextModelRunner>(...) with empty tensor map
- Call alloc_params_buffer()
- Call get_desc() to verify

**Example 2: T5 Model (both variants)**
- Show creating "t5-standard" variant
- Show creating "t5-umt5" variant
- Demonstrate version checking before creation

**Example 3: WAN VAE (encode and decode)**
- Show "wan-vae-t2v-encode" runner creation
- Show "wan-vae-t2v-decode" runner creation
- Note decode-only pattern for tests

**Example 4: WAN Transformer**
- Show "wan-transformer-t2v" runner creation
- Show layer count configuration pattern

Include:
- #include statements for model_registry.hpp, clip.hpp, t5.hpp, wan.hpp
- Error handling with try/catch for unknown versions
- Comments explaining each step
- Cleanup/reset patterns between examples
  </action>
  <verify>
File compiles (syntax check), includes all 4 model types, shows version checking and error handling.
  </verify>
  <done>docs/examples/model_registry_usage.cpp created with 4 complete examples covering all model types</done>
</task>

<task type="auto">
  <name>Task 3: Create I/O utilities usage examples</name>
  <files>docs/examples/io_utils_usage.cpp</files>
  <action>
Create docs/examples/io_utils_usage.cpp with runnable examples extracted from test_io_npy.cpp patterns:

**Example 1: Round-trip save and load**
- Create GGMLCtxRAII context
- Create a 1D tensor with F32 data
- Call save_npy(path, tensor)
- Call load_npy(path, ctx) to reload
- Verify data matches

**Example 2: Multi-dimensional tensors**
- Show 2D tensor (matrix) save/load
- Show 3D tensor save/load
- Demonstrate dimension mapping (ggml ne[] reversed from NumPy)

**Example 3: Different dtypes**
- Show F32 (float32) example
- Show F16 (float16) example
- Show I32 (int32) example
- Show I64 (int64) example

**Example 4: Error handling**
- Try loading non-existent file (catch exception)
- Try loading unsupported dtype (catch exception)
- Show error messages

Include:
- #include statements for test_io_utils.hpp, ggml.h, filesystem
- TempFile pattern for cleanup
- Comments explaining dimension mapping
- Precision comparison for float types
  </action>
  <verify>
File compiles (syntax check), shows all 4 dtypes, demonstrates error handling, includes dimension mapping explanation.
  </verify>
  <done>docs/examples/io_utils_usage.cpp created with 4 complete examples covering all dtypes and error cases</done>
</task>

</tasks>

<verification>
- docs/API_CALLS.md exists and contains both model registry and I/O sections
- All 9 registered models listed with correct version strings
- All API functions documented with signatures and parameters
- docs/examples/model_registry_usage.cpp compiles and shows all 4 model types
- docs/examples/io_utils_usage.cpp compiles and shows all 4 dtypes
- Examples include error handling and lifecycle patterns
</verification>

<success_criteria>
Developers can copy-paste examples from docs/examples/ directly into their code and understand:
1. How to create any of the 9 registered models
2. How to load/save .npy files with different dtypes
3. How to manage backend and context lifecycles
4. How to handle errors and version checking
</success_criteria>

<output>
After completion, create `.planning/quick/260328-m62-api/260328-m62-SUMMARY.md`
</output>
