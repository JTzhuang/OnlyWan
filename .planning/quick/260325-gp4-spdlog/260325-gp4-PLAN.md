---
phase: 16-spdlog
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [src/util.h, src/util.cpp, src/api/wan-api.cpp, src/model.cpp, src/model.h, src/rope.hpp, src/t5.hpp, src/ggml_extend.hpp, src/clip.hpp, src/tokenize_util.cpp]
autonomous: true
requirements: [LOG-01]
---

<objective>
Replace all direct logging (printf, std::cout, std::cerr, fprintf, std::endl) in src/ with spdlog calls through the centralized LOG_* macros in util.h.

Purpose: Unified, configurable, and high-performance logging system.
Output: Cleaned up source code using spdlog for all diagnostics.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/STATE.md
@src/util.h
@src/util.cpp
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update util.h/cpp with centralized logger and initialization</name>
  <files>src/util.h, src/util.cpp</files>
  <action>
    - In src/util.h: Ensure LOG_DEBUG, LOG_INFO, LOG_WARN, LOG_ERROR macros are available and use log_printf.
    - In src/util.cpp:
      - Implement or refine `get_wan_logger()` to initialize spdlog once.
      - Update `log_printf` to handle spdlog output.
      - Add a `wan_log_init()` function if needed, or rely on `get_wan_logger()`'s `std::call_once`.
      - Replace direct `printf` in `pretty_progress` with `fflush(stdout)` and `printf` (or keep as is if progress bar needs raw terminal control, but prefer consistent logging).
  </action>
  <verify>
    <automated>grep -E "LOG_(INFO|WARN|ERROR|DEBUG)" src/util.h</automated>
  </verify>
  <done>Logging macros are ready and backed by spdlog.</done>
</task>

<task type="auto">
  <name>Task 2: Replace direct logging in src/ files</name>
  <files>src/api/wan-api.cpp, src/model.cpp, src/model.h, src/rope.hpp, src/t5.hpp, src/ggml_extend.hpp, src/clip.hpp, src/tokenize_util.cpp, src/util.cpp</files>
  <action>
    - Iterate through the identified files and replace:
      - `printf(...)` -> `LOG_INFO(...)` (or DEBUG/WARN based on context)
      - `fprintf(stderr, ...)` -> `LOG_WARN(...)` or `LOG_ERROR(...)`
      - `std::cout << ... << std::endl` -> `LOG_INFO(...)`
      - `std::cerr << ... << std::endl` -> `LOG_ERROR(...)`
    - In src/api/wan-api.cpp: Remove the temporary debug logging to `/tmp/wan_debug.log` and replace with `LOG_DEBUG` or `LOG_INFO`.
    - Ensure `#include "util.h"` is present in all modified files.
  </action>
  <verify>
    <automated>grep -rE "printf|std::cout|std::cerr|fprintf\(stderr" src/ | grep -v "thirdparty" | grep -v "util.cpp" | wc -l</automated>
  </verify>
  <done>All direct logging calls in src/ (excluding util core) are replaced by macros.</done>
</task>

</tasks>

<verification>
Check that the code compiles and logs appear in the expected spdlog format.
</verification>

<success_criteria>
- No direct printf/cout/cerr in project source code (except util.cpp logging core).
- All logs go through spdlog.
- Log levels are correctly mapped (stderr -> warn/error, stdout -> info).
</success_criteria>

<output>
After completion, create .planning/phases/16-spdlog/16-01-SUMMARY.md
</output>
