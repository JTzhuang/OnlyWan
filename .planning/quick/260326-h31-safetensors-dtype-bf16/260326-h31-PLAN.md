---
phase: quick
plan: 260326-h31-safetensors-dtype-bf16
type: execute
wave: 1
depends_on: []
files_modified: [scripts/merge_safetensors_bf16.py, tests/scripts/test_merge_safetensors_bf16.py]
autonomous: true
requirements: []
must_haves:
  truths:
    - "用户可以把多个 .safetensors 输入文件合并成单个输出文件"
    - "输出文件中的所有浮点张量都以 bfloat16 保存"
    - "输入文件中的非浮点张量会被保留，不会被错误转换或丢失"
    - "键名冲突时脚本会直接报错，不会静默覆盖已有张量"
  artifacts:
    - path: "scripts/merge_safetensors_bf16.py"
      provides: "命令行合并脚本，读取多个 safetensors、转成 bf16、输出单文件"
      min_lines: 40
    - path: "tests/scripts/test_merge_safetensors_bf16.py"
      provides: "覆盖合并、bf16 转换、非浮点保留、重复键报错的自动化测试"
      min_lines: 60
  key_links:
    - from: "tests/scripts/test_merge_safetensors_bf16.py"
      to: "scripts/merge_safetensors_bf16.py"
      via: "subprocess 调用 CLI"
      pattern: "subprocess\\.run.*merge_safetensors_bf16\\.py"
    - from: "scripts/merge_safetensors_bf16.py"
      to: "safetensors.torch.load_file"
      via: "逐个加载输入文件"
      pattern: "load_file"
    - from: "scripts/merge_safetensors_bf16.py"
      to: "safetensors.torch.save_file"
      via: "保存最终合并结果"
      pattern: "save_file"
---

<objective>
实现一个原子化 Python CLI 脚本，把多个 `.safetensors` 权重文件合并成一个输出文件，并将所有浮点张量统一转换为 `bfloat16`。

Purpose: 让已有多分片或多来源 safetensors 权重可以直接合并为单文件，同时满足 bf16 推理/分发需求，避免手工逐个转换。
Output: 一个新的合并脚本和一组可重复运行的自动化测试。
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/data/zhongwang2/jtzhuang/projects/OnlyWan/.planning/STATE.md
@/data/zhongwang2/jtzhuang/projects/OnlyWan/docs/superpowers/specs/2026-03-26-pth-to-safetensors-converter-design.md
@/data/zhongwang2/jtzhuang/projects/OnlyWan/scripts/pth_to_safetensors.py
@/data/zhongwang2/jtzhuang/projects/OnlyWan/tests/scripts/test_pth_to_safetensors.py

Existing pattern to follow:
- `scripts/pth_to_safetensors.py` already uses argparse + torch + `safetensors.torch.save_file`
- `tests/scripts/test_pth_to_safetensors.py` already uses subprocess-based CLI tests with temporary files
- This quick task is not a `.pth` converter extension; it is a separate safetensors-to-safetensors merge utility, so keep the new CLI isolated instead of overloading the existing converter script
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: 新增 safetensors 合并与 bf16 转换测试</name>
  <files>tests/scripts/test_merge_safetensors_bf16.py</files>
  <behavior>
    - Test 1: 两个输入文件包含不同张量键时，脚本成功生成单个输出文件，输出中同时包含两边的键。
    - Test 2: 输入中的 float32/float16 张量在输出里统一变成 torch.bfloat16。
    - Test 3: 输入中的 int64 或 bool 张量在输出里保持原始 dtype，不被错误转成 bf16。
    - Test 4: 两个输入文件出现相同键名时，脚本非零退出并给出重复键错误，避免静默覆盖。
  </behavior>
  <action>创建基于 subprocess 的 pytest 用例，使用 `safetensors.torch.save_file` 在临时目录中构造多个输入文件，并调用 `scripts/merge_safetensors_bf16.py`。测试要直接校验输出文件能被 `load_file` 读取、键集合正确、浮点 dtype 全部为 bf16、非浮点 dtype 保持不变。重复键场景必须断言命令失败并匹配明确的 collision/duplicate key 报错文案。</action>
  <verify>
    <automated>pytest /data/zhongwang2/jtzhuang/projects/OnlyWan/tests/scripts/test_merge_safetensors_bf16.py -q</automated>
  </verify>
  <done>测试文件稳定复现目标行为，先覆盖成功合并、bf16 转换、非浮点保留和重复键失败四个核心场景。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: 实现多文件合并并统一转为 bf16 的 CLI 脚本</name>
  <files>scripts/merge_safetensors_bf16.py</files>
  <behavior>
    - 给定多个输入 `.safetensors` 路径和一个输出路径时，脚本会按输入顺序加载并合并所有张量。
    - 所有浮点张量在写出前统一转换为 `torch.bfloat16`。
    - 非浮点张量原样保留。
    - 任一输入文件与已合并内容存在重复键时，脚本立即退出并返回非零状态。
  </behavior>
  <action>新增独立脚本 `scripts/merge_safetensors_bf16.py`，沿用现有 Python CLI 风格：使用 `argparse` 接收多个输入文件和一个 `--output` 参数；使用 `safetensors.torch.load_file` 逐个在 CPU 上读取；对每个 tensor 仅在 `is_floating_point()` 时执行 `.to(torch.bfloat16)`；把转换后的键值写入总字典；发现重复键时抛出带键名和来源文件的 `ValueError`；最后用 `safetensors.torch.save_file` 输出单个 safetensors 文件。不要修改 `scripts/pth_to_safetensors.py`，避免把 `.pth` 转换逻辑和 safetensors 合并逻辑耦合在一起。</action>
  <verify>
    <automated>pytest /data/zhongwang2/jtzhuang/projects/OnlyWan/tests/scripts/test_merge_safetensors_bf16.py -q</automated>
  </verify>
  <done>脚本可直接从命令行合并多个 safetensors 输入文件为单一输出文件，浮点张量统一变为 bf16，重复键不会被静默覆盖。</done>
</task>

<task type="auto">
  <name>Task 3: 运行一次端到端 smoke check 固化 CLI 用法</name>
  <files>scripts/merge_safetensors_bf16.py, tests/scripts/test_merge_safetensors_bf16.py</files>
  <action>在测试之外增加一次面向执行者的端到端验证约束：确保脚本帮助信息包含多个输入文件和 `--output` 的用法；确保测试中的 happy-path 场景等价于真实 CLI 使用方式，例如 `python3 scripts/merge_safetensors_bf16.py a.safetensors b.safetensors --output merged.safetensors`。保持实现原子，不增加额外配置文件、文档或通用 dtype 选项，本任务只交付 bf16 合并能力。</action>
  <verify>
    <automated>python3 /data/zhongwang2/jtzhuang/projects/OnlyWan/scripts/merge_safetensors_bf16.py --help >/dev/null && pytest /data/zhongwang2/jtzhuang/projects/OnlyWan/tests/scripts/test_merge_safetensors_bf16.py -q</automated>
  </verify>
  <done>CLI 入口可发现、参数语义清晰，自动化测试和命令行帮助共同证明该工具已经可执行。</done>
</task>

</tasks>

<verification>
确认以下事实同时成立：
- `scripts/merge_safetensors_bf16.py` 能接受多个输入 safetensors 文件并输出一个合并文件
- 输出内所有浮点 tensor dtype 均为 `torch.bfloat16`
- int/bool 等非浮点 tensor dtype 未被破坏
- 键冲突时报错退出，没有 silent overwrite
- 自动化测试可在本仓库中直接运行通过
</verification>

<success_criteria>
- 执行 `pytest /data/zhongwang2/jtzhuang/projects/OnlyWan/tests/scripts/test_merge_safetensors_bf16.py -q` 通过
- 执行 `python3 /data/zhongwang2/jtzhuang/projects/OnlyWan/scripts/merge_safetensors_bf16.py --help` 成功
- 生成的输出 safetensors 可被 `safetensors.torch.load_file` 读取
- 至少一个测试明确验证重复键时报错而不是覆盖
</success_criteria>

<output>
After completion, create `/data/zhongwang2/jtzhuang/projects/OnlyWan/.planning/quick/260326-h31-safetensors-dtype-bf16/260326-h31-SUMMARY.md`
</output>
