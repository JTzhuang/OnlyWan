---
quick_task: 260328-v10
description: 为 benchmark_inference 工具添加 --device 参数来设置 GPU 卡号
type: execute
autonomous: true
---

<objective>
Add `--device` parameter to benchmark_inference tool to allow users to select specific GPU device ID when using CUDA backend.

Purpose: Enable multi-GPU benchmarking and device selection without code changes
Output: Updated benchmark_inference.cpp with --device parameter support
</objective>

<context>
Current state (from benchmark_inference.cpp):
- Line 114-135: backend_from_string() function hardcodes device 0 for CUDA (line 122: `ggml_backend_cuda_init(0)`)
- Line 454-478: Command-line argument parsing in main()
- Line 459: backend variable initialized to "cpu"
- Line 481-488: Help text shows available options

Key insight: ggml_backend_cuda_init() accepts device_id parameter. Need to:
1. Add --device parameter parsing
2. Pass device_id to backend_from_string()
3. Update help text
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add --device parameter parsing and backend selection</name>
  <files>tests/cpp/benchmark_inference.cpp</files>
  <action>
Modify benchmark_inference.cpp to support --device parameter:

1. Add device_id variable after line 459:
   - int device_id = 0;  // default to device 0

2. Add --device argument parsing in the loop (after line 476):
   - else if (arg == "--device" && i + 1 < argc) {
   -     device_id = std::stoi(argv[++i]);
   - }

3. Modify backend_from_string() signature (line 114):
   - Change: ggml_backend_t backend_from_string(const std::string& backend_name)
   - To: ggml_backend_t backend_from_string(const std::string& backend_name, int device_id = 0)

4. Update CUDA backend initialization (line 122):
   - Change: return ggml_backend_cuda_init(0);
   - To: return ggml_backend_cuda_init(device_id);

5. Update all backend_from_string() calls to pass device_id:
   - Lines 143, 215, 287, 356: Change backend_from_string(backend_name) to backend_from_string(backend_name, device_id)

6. Update help text (line 481-488):
   - Add new line after --backend option:
   - "  --device ID                             GPU device ID for CUDA backend (default: 0)\n"
  </action>
  <verify>
    <automated>grep -n "device_id" tests/cpp/benchmark_inference.cpp | head -10</automated>
  </verify>
  <done>
- --device parameter parsed from command line
- device_id passed to backend_from_string()
- CUDA backend initialized with correct device ID
- Help text documents --device option
- All benchmark functions use device_id when selecting backend
  </done>
</task>

</tasks>

<verification>
After implementation:
1. Verify parameter parsing: grep shows device_id variable and argument handling
2. Verify backend function signature updated with device_id parameter
3. Verify all benchmark_*() calls pass device_id to backend_from_string()
4. Verify help text includes --device option
</verification>

<success_criteria>
- benchmark_inference --help shows --device option
- benchmark_inference --backend cuda --device 1 initializes CUDA device 1
- Default behavior (no --device) uses device 0
- All four benchmark functions (clip, t5, vae, transformer) respect device selection
</success_criteria>

<output>
After completion, create `.planning/quick/260328-v10-benchmark-inference-device-gpu/260328-v10-SUMMARY.md`
</output>
