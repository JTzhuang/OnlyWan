---
phase: quick
plan: 260328-uo6
type: execute
wave: 1
depends_on: []
files_modified: [tests/cpp/benchmark_inference.cpp]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "benchmark_inference compiles with WAN_CUDA=ON"
    - "Backend selection correctly detects CUDA availability at runtime"
    - "--backend cuda flag works when compiled with CUDA support"
  artifacts:
    - path: "tests/cpp/benchmark_inference.cpp"
      provides: "Corrected preprocessor conditionals for backend detection"
      contains: "WAN_USE_CUDA, WAN_USE_METAL"
  key_links:
    - from: "CMakeLists.txt (line 56: add_definitions(-DWAN_USE_CUDA))"
      to: "tests/cpp/benchmark_inference.cpp (backend_from_string function)"
      via: "Preprocessor define matching"
      pattern: "WAN_USE_CUDA"
---

<objective>
Fix benchmark_inference tool to properly detect and use CUDA backend when compiled with WAN_CUDA=ON.

Purpose: Enable CUDA-accelerated benchmarking by correcting preprocessor define mismatches between CMakeLists.txt and the benchmark tool.
Output: benchmark_inference binary that correctly selects CUDA backend when available.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@CMakeLists.txt (lines 34-106: backend option handling and define mapping)
@tests/cpp/benchmark_inference.cpp (lines 106-127: backend_from_string function)

Current issue: backend_from_string() checks for SD_USE_CUDA and SD_USE_METAL (lines 111-122), but CMakeLists.txt defines WAN_USE_CUDA and WAN_USE_METAL (lines 56, 67). This mismatch prevents CUDA backend from being detected even when compiled with -DWAN_USE_CUDA.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix preprocessor conditionals in backend_from_string()</name>
  <files>tests/cpp/benchmark_inference.cpp</files>
  <action>
Update backend_from_string() function (lines 106-127) to use correct preprocessor defines:
- Replace #ifdef SD_USE_CUDA with #ifdef WAN_USE_CUDA (line 111)
- Replace #ifdef SD_USE_METAL with #ifdef WAN_USE_METAL (line 117)

This aligns with CMakeLists.txt which defines WAN_USE_CUDA (line 56) and WAN_USE_METAL (line 67) when respective backends are enabled.

After fix, the function will correctly detect CUDA/Metal availability at compile time and allow --backend cuda/metal flags to work when compiled with WAN_CUDA=ON or WAN_METAL=ON.
  </action>
  <verify>
    <automated>grep -n "WAN_USE_CUDA\|WAN_USE_METAL" /data/zhongwang2/jtzhuang/projects/OnlyWan/tests/cpp/benchmark_inference.cpp | head -5</automated>
  </verify>
  <done>
Lines 111 and 117 in benchmark_inference.cpp use WAN_USE_CUDA and WAN_USE_METAL respectively, matching CMakeLists.txt defines. No SD_USE_* references remain in backend_from_string().
  </done>
</task>

</tasks>

<verification>
After task completion:
1. Verify preprocessor defines match: grep shows WAN_USE_CUDA and WAN_USE_METAL in backend_from_string()
2. Compile test: `cd /data/zhongwang2/jtzhuang/projects/OnlyWan && rm -rf build && mkdir build && cd build && cmake -DWAN_CUDA=ON -DWAN_BUILD_TESTS=ON .. && cmake --build . --target benchmark_inference` should succeed
3. Runtime test: `./build/bin/benchmark_inference --backend cuda --help` should show CUDA backend available (no fallback message)
</verification>

<success_criteria>
- benchmark_inference.cpp lines 111 and 117 use WAN_USE_CUDA and WAN_USE_METAL
- No SD_USE_* preprocessor checks remain in the file
- Tool compiles successfully with WAN_CUDA=ON
- Backend selection correctly identifies CUDA when available
</success_criteria>

<output>
After completion, create `.planning/quick/260328-uo6-benchmark-inference-cuda-wan-cuda/260328-uo6-SUMMARY.md`
</output>
